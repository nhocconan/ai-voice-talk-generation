global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: voice_studio_web
    static_configs:
      - targets:
          - host.docker.internal:3000
    metrics_path: /api/metrics

  - job_name: prometheus
    static_configs:
      - targets:
          - localhost:9090
